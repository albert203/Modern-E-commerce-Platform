document.addEventListener('DOMContentLoaded', async () => {
    profileButton = document.querySelector('.profile-edit-button');
    profileButton.document.addEventListener('click', async () => {

    });
});